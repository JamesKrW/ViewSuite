envs:
  - name: MindCubeNoToolGym
    data_source: mindcube
    seed: [0,1050,1]
    n_envs: 1050
    max_turns: 1
    response_length_per_turn: 2048
    config:
      data_path: ${oc.env:VIEWSUITE_ROOT}/data/mindcube
      split: dev
      image_size: [512,512]
      format: eval_mode
      format_reward: 0.2
      answer_reward: 0.8
